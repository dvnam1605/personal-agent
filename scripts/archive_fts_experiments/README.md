# Archived FTS experiments

One-off exploration/verification scripts used while building the Vietnamese
FTS stack (`vietnamese_simple` + unaccent) during P9E and P10A.

They are superseded by:

- `alembic/versions/0007_add_fts_to_document_chunks.py` (migration deliverable)
- `tests/unit/services/test_retrieval_foundation.py` (regression coverage)

Kept for historical reference only — do not import anything here from `app/`.
